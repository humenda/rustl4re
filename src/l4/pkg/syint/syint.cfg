-- vim:set ft=lua:

local L4 = require("L4"); -- Include L4 functionality

-- Some shortcut for less typing
local ld = L4.default_loader;

ld:start({ caps = { pfc = L4.Env.icu },
         log = { "syint", "blue" } },
         "rom/syint");

-- L4.cast("L4::Platform_control", L4.Env.icu):system_shutdown(0);
